= form_with model: form.packaging, local: true do |f|
  = render 'shared/error_messages', resource: form.packaging if form.errors.any?

  div
    = f.label :name
    br
    = f.text_field :name

  div
    = f.button :submit
